import { InternalPactVerifierOptions } from '../types';
import { Ffi, FfiVerifierHandle } from '../../ffi/types';
export declare const setupVerification: (ffi: Ffi, handle: FfiVerifierHandle, options: InternalPactVerifierOptions) => void;
